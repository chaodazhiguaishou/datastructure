#include <iostream>
#include <stdio.h>
#include <algorithm>
using namespace std;

//二叉树顺序存储结构采用字符串（假设每个结点值为单个字符）或者数组存放。
//当二叉树中某结点为空结点或无效结点（不存在该编号的结点）时，对应位置的值用特殊值（如'#'）表示。

//优缺点：
//完全二叉树或满二叉树采用顺序存储结构比较合适
//如果增加很多空结点才能将一棵二叉树改造成为一棵完全二叉树，采用顺序存储结构会造成空间的大量浪费，这时不宜用顺序存储结构。 


class Solution
{
public:

};

int main() {
    // insert code here...
    
};