#include <iostream>
#include <stdio.h>
using namespace std;

// 1. 采用开放定址法建立的哈希表的查找

// int search(int k) ////1. 采用开放定址法建立的哈希表的查找
// {
//     int d = k % p; // 求哈希函数值
//     while (ha[d].key != NULLKEY && ha[d].key != k)
//     {
//         d = (d + 1) % m; // 用线性探测法查找空位置
//     }
//     if (ha[d].key == k) // 查找成功返回
//     {
//         return d;
//     }
//     else
//         return -1;
// }

// 2. 采用拉链法建立的哈希表的查找

int main(){
    // insert code here...
};